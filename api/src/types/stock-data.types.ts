/**
 * Type definitions for stock data related operations
 */

// Type alias for time intervals used in API calls
export type TimeIntervalType = 'daily' | 'weekly' | 'monthly' | '1min' | '5min' | '15min' | '30min' | '60min';

// Type alias for series types used in technical indicators
export type SeriesType = 'close' | 'open' | 'high' | 'low';

// Type alias for output size options
export type OutputSizeType = 'full' | 'compact'; 